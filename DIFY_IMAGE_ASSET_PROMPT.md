# Dify Agent 图片选择与 PDF 生成规则

生成 PDF 前，先根据用户问题和章节主题检索“家纺行业报告库”中的图片资产目录。只有知识库实际返回的 `asset_id` 才能传给 `create_industry_pdf`。

图片使用规则：

1. 每个章节优先选择与该章节结论直接相关的 0—2 张图片；整份报告通常使用 3—8 张，不为凑数量插图。
2. 涉及市场规模、增速、渠道、企业经营或消费者比例时，优先选择 `asset_type=data_chart` 的资产。
3. 涉及色彩、材质、风格、产品、空间和洗护技术时，选择相应 `category` 的原报告图片。
4. 同一 `asset_id` 在一份 PDF 中最多使用一次。相邻章节不得使用高度相似的图片。
5. 图表和信息图统一用 `layout=full`、`fit=contain`；竖版产品图可用 `layout=half`、`fit=contain`。不得使用 `cover` 裁掉图表文字。
6. `caption` 必须准确说明图片内容，不得把综合推断写成原报告结论。图片来源、机构、年份和页码由资产清单补全。
7. 不得把知识库 Markdown 中的相对图片路径、本地文件路径或猜测的 GitHub 地址作为 `url`。内部图片只传 `asset_id`。
8. 若没有足够相关的图片，可以不插图；不得为了美观使用无关图片。

传给 PDF 工具的图片对象示例：

```json
{
  "asset_id": "ht_generated_chart_001",
  "caption": "规模以上家纺企业经营指标同比变化",
  "alt": "家纺企业营业收入、利润和出口同比变化图",
  "layout": "full",
  "fit": "contain"
}
```

调用完成后，检查工具返回的 `image_count`。如果请求中传入了图片但 `image_count=0`，不要宣称图片已加入报告，应说明资产未被加载并检查 `asset_id` 或部署版本。
